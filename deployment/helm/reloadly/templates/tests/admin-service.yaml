apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reloadly.fullname" . }}-admin-service-test"
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['admin-service:{{ .Values.reloadly.adminApp.http.port }}/actuator/health']
  restartPolicy: Never
